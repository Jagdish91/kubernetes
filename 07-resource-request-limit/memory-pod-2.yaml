apiVersion: v1
kind: Pod
metadata:
  name: memory-pod-2
spec:
  containers:
  - image: polinux/stress
    name: memory-pod-2
    resources:
      requests:
        memory: "100Mi"
      limits:
        memory: "200Mi"
    command: ["stress"]
    args: [ "--vm", "1", "--vm-bytes", "250M", "--vm-hang", "1" ]
